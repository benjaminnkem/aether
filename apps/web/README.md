# Aether Web

The Next.js App Router frontend for the focused Aether MVP. Run from the repository
root with `pnpm --filter @aether/web dev`. Browser data always comes from the
authenticated NestJS API. See the root `README.md`, `docs/ARCHITECTURE.md`, and
`docs/03_AETHER_UI_UX_SPEC.md`.
