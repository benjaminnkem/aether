# Aether Web

The Next.js App Router frontend for the focused Aether MVP. Run from the repository
root with `pnpm --filter @aether/web dev`. Browser data defaults to deterministic mock
mode; see the root `README.md` and `docs/FRONTEND_ARCHITECTURE.md`.
