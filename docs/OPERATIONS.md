# Operations

The demo operation uses this lifecycle: observe → investigate → plan → policy → simulate → approve → KeeperHub execute → finality → independently verify → notify. Partial writes produce a linked forward-correction state, never a fictional rollback. The development scenario controller provides deterministic operator rehearsal.
