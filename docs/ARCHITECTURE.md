# Architecture

Phase 1 implements the frontend boundaries described in `docs/02_AETHER_SYSTEM_ARCHITECTURE.md`. See `docs/FRONTEND_ARCHITECTURE.md` for package ownership, state rules, dynamic loading, and the mock/API adapter switch. Backend, worker, persistence, queue, and contract architecture remain specified but unimplemented.
